>div.content-wrapper.{{active}}
    >textarea#inputArea
        @blur = blurEvent
        @focus = focusEvent
        @input = inputEvent
        #placeholder = {{contentUI}}
        #rows = 4
        #id = area
        #autocomplete = new-password
        %disabled = {{disabled}}
        %value = {{content}}